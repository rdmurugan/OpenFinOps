# OpenFinOps Core Dependencies
# ============================

# Core dependencies
numpy>=1.24.0
matplotlib>=3.7.0
tornado>=6.0.0
flask>=2.0.0
requests>=2.28.0
pyyaml>=6.0

# Optional: Install with pip install openfinops[feature]
# For cloud providers: openfinops[aws,azure,gcp]
# For AI platforms: openfinops[openai,anthropic]
# For databases: openfinops[postgres,mongodb]
# For everything: openfinops[all]
